// ─────────────────────────────────────────────────────────────────
// この API route は現在未使用です。
// フォーム送信は Web3Forms（クライアントサイド）経由で処理しています。
// 将来的にサーバーサイド処理が必要になった場合に備えて残しています。
// ─────────────────────────────────────────────────────────────────

import { NextResponse } from 'next/server'

export async function POST() {
  return NextResponse.json(
    { message: 'このエンドポイントは現在使用されていません。フォームはWeb3Forms経由で送信されます。' },
    { status: 410 }
  )
}
